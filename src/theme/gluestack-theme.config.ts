/**
 * Gluestack UI Theme Configuration
 *
 * This file defines the theme configuration for Gluestack UI components.
 * It supports both light and dark modes, which will be automatically
 * applied based on the system color scheme preferences.
 *
 * For more customization options, refer to:
 * https://gluestack.io/ui/docs/home/overview/quick-start
 */

// Basic theme configuration for Gluestack UI
// The provider will use default theme if no config is provided
// This file can be extended with custom theme tokens as needed

export const gluestackThemeConfig = {
  // Theme tokens can be customized here
  // For now, we'll use the default theme provided by Gluestack UI
  // which automatically supports light and dark modes
};
